# import sqlite3 as sql
#
# conn = sql.connect("D:/Works/RASAs/rasa0/Database/rasa.db")
# cu = conn.cursor()
# cu.execute("create table USER (acc varchar(30) primary key, password varchar(30), balance integer)")
# conn.execute("insert into USER(acc, password, balance) values ('test', 'test', 1000)")
# conn.commit()
